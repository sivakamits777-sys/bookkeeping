

export const FIREBASE_PROJECT_ID = process.env.PROJECT_ID || "";
export const FIREBASE_LOCATION = process.env.GOOGLE_CLOUD_LOCATION || "us-central1";

// SQL_INIT_SCRIPT is removed as we are now using Firebase Firestore.;
